# Encryption
### The Monad(a) Engine's tool
Encrypts a file by creating a binary file with a SHA256 and using a salt depended on a project.
(Encrypted files are rather not copyable between different projects)