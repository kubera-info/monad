# Time Module

This module provides time measurement functionality for the application.

Time is measured using the **double** type.
The time flow can be started, stopped, or paused.

The module also provides a class for linear animations.
This **lerp** operates in seconds using **double** precision.

The lerp system supports events,
which can be triggered when the animation duration has completed.