# The module Inet

Inet uses **libCurl** for connecting with a server through the encrypted **HTTP**.

It uses official build, wich is downloaded automatically. This process do not alter other code,
and when a new version is available, the developer should correct new settings
and the source code.